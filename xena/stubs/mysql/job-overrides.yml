job_overrides:
  colocated_jobs:
    proxy_z1:
      additional_templates:
        - name: consul_agent
          release: cf
          consumes: {consul: nil}
    proxy_z2:
      additional_templates:
        - name: consul_agent
          release: cf
          consumes: {consul: nil}
  additional_releases:
  - name: cf
    version: (( release_versions.cf.version || "latest" ))
