---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfplatformrecovery/backup-and-restore-cf-for-k8s

inputs:
- name: ci
- name: cluster-name
- name: velero-bucket

params:
  GKE_SERVICE_ACCOUNT_KEY:
  VELERO_SERVICE_ACCOUNT_KEY:
  VELERO_TEST_BUCKET:

run:
  path: ci/ci/backup-metadata/install-velero.sh
