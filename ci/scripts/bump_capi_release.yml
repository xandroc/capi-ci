---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/runtime-ci}

inputs:
  - name: cloud_controller_ng
  - name: capi-release
  - name: capi-ci

outputs:
  - name: bumped

run:
  path: capi-ci/ci/scripts/bump_capi_release.sh

params:
  SOURCE_REPO:
  DESTINATION_SUBMODULE: