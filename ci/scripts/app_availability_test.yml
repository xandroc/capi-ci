---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/capi
    tag: runtime-ci

inputs:
  - name: cf-acceptance-tests
  - name: capi-ci
  - name: capi-ci-private
  - name: cloud_controller_ng

run:
  path: capi-ci/ci/scripts/app_availability_test.sh

params:
  APP_DOMAIN:
  API_DOMAIN:
  CLOUD_CONTROLLER_BRANCH:
  TUNNEL_HOST:
  DB_HOST:
  ENVIRONMENT:
  DB_USERNAME:
  DB_PASSWORD:
  DB_NAME:
  DB_PORT:
  DB:
